namespace OpenSorSe.Desktop.ViewModels;

/// <summary>Identifies local sections inside the consolidated Saved scans area.</summary>
public enum SavedScansSection
{
    /// <summary>Lists retained scan summaries.</summary>
    Library,

    /// <summary>Searches retained scan metadata.</summary>
    Search,

    /// <summary>Compares two retained scans.</summary>
    Compare,
}
